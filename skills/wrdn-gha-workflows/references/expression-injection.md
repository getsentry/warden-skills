# Expression Injection

Use this reference when `${{ }}` expressions appear inside `run:` blocks or composite-action shell steps.

## Core Rule

GitHub expression expansion happens before the shell runs. If attacker-controlled context is placed directly in a shell script, the attacker can inject shell syntax.

## Attacker-Controlled Values

Treat these as untrusted when the trigger is externally reachable:

- `github.event.pull_request.title`
- `github.event.pull_request.body`
- `github.event.pull_request.head.ref`
- `github.event.pull_request.head.label`
- `github.head_ref`
- `github.event.issue.title`
- `github.event.issue.body`
- `github.event.comment.body`
- `github.event.review.body`
- `github.event.discussion.title`
- `github.event.discussion.body`
- `github.event.label.name`
- `github.event.pages.*.page_name`
- filenames and changed-file lists produced from PR-controlled refs
- commit messages and author fields from untrusted refs
- action inputs supplied by an externally reachable workflow

Usually safe or low-risk:

- `github.event.pull_request.number`
- full commit SHAs
- booleans
- base repository owner/name constants
- values generated by prior trusted steps

## Dangerous Contexts

Report when untrusted expressions land in:

- `run: echo "${{ github.event.pull_request.title }}"`
- loops over changed filenames injected through `${{ steps.changed.outputs.files }}`
- heredocs without safe quoting
- shell commands composed inside composite actions
- `bash -c`, `sh -c`, `python -c`, `node -e`
- command arguments later passed to `eval`, `exec`, `os.system`, `child_process.exec`, or equivalent

Do not report expression use in `if:` or `with:` by default. They can become dangerous only when the receiving action or later shell step interprets the value as code.

## Verification Steps

1. Confirm the workflow trigger is externally reachable.
2. List every expression inside `run:`.
3. Classify whether each expression is attacker-controlled.
4. Check whether the value is passed through an environment variable and quoted at the shell use site. `env:` alone is not enough if the later command uses `$VALUE` unquoted or feeds it to `eval`.
5. Follow composite action inputs to their shell steps.
6. Drop findings for numeric IDs, fixed SHAs, and base-repository constants unless later code reinterprets them unsafely.

## Fix Patterns

- Move untrusted expression values into `env:`.
- Quote shell variables: `printf '%s\n' "$PR_TITLE"`.
- Parse changed-file lists with a trusted action output format or a checked-in script that treats filenames as data.
- Validate action inputs before command construction.
- Use argv APIs in scripts instead of shell strings.
- Avoid `eval`, `bash -c`, and `exec` with untrusted strings.
