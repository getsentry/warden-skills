name: Build
on:
  pull_request_target:
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PR_BODY: ${{ github.event.pull_request.body }}
    steps:
      - run: |
          eval "echo $PR_BODY"
