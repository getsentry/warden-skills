name: Lint PR Title
on:
  pull_request_target:
    types: [opened, edited]
jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Validate title
        run: |
          echo "Title: ${{ github.event.pull_request.title }}"
