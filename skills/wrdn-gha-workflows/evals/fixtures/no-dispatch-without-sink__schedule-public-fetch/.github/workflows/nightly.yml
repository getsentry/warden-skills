name: Nightly Stats
on:
  schedule:
    - cron: '0 3 * * *'
permissions:
  contents: read
jobs:
  stats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Count files
        run: find . -type f | wc -l
