on:
  issue_comment:
    types: [created]

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show PR number
        env:
          PR_NUM: ${{ github.event.issue.number }}
        run: echo "PR is $PR_NUM"
