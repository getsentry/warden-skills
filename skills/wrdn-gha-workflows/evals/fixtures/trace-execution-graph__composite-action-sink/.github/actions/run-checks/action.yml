name: 'Run Checks'
description: 'Runs project checks'
inputs:
  title:
    description: 'PR title'
    required: true
runs:
  using: 'composite'
  steps:
    - name: Log title
      shell: bash
      run: |
        echo "Checking PR: ${{ inputs.title }}"
        ./scripts/lint.sh
