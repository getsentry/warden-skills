name: PR
on:
  pull_request_target:
    types: [opened, synchronize]
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run local greeter
        uses: ./.github/actions/greeter
        with:
          title: ${{ github.event.pull_request.title }}
