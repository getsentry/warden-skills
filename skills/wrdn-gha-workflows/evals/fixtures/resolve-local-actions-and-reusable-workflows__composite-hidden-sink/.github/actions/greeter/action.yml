name: Greeter
description: Greets a PR
inputs:
  title:
    description: PR title
    required: true
runs:
  using: composite
  steps:
    - name: Print greeting
      shell: bash
      run: |
        echo "Hello, processing PR: ${{ inputs.title }}"
