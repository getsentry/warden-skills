name: PR Info
on:
  pull_request:
permissions:
  contents: read
  pull-requests: read
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print PR title
        run: echo "Title is ${{ github.event.pull_request.title }}"
