name: PR Check
on:
  pull_request_target:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - run: ./scripts/process-pr.sh
        env:
          DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
