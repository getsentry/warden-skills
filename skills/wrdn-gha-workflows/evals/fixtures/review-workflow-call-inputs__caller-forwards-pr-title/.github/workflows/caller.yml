name: Caller
on:
  pull_request_target:
    types: [opened, edited]
jobs:
  call-build:
    uses: ./.github/workflows/reusable-build.yml
    with:
      message: ${{ github.event.pull_request.title }}
