name: Comment
on:
  pull_request_target:
    types: [opened]
jobs:
  comment:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Log PR number
        run: echo "Processing PR #${{ github.event.pull_request.number }}"
