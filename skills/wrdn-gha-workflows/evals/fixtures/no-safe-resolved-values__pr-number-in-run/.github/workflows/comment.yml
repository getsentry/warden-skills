name: Comment
on:
  pull_request_target:
    types: [opened]
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Echo PR number
        run: echo "PR #${{ github.event.pull_request.number }} opened"
