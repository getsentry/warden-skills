name: Deploy
on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Echo target
        run: echo "Deploying to ${{ inputs.environment }}"
