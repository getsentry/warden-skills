name: Triage
on:
  issue_comment:
    types: [created]

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: Process comment
        run: |
          echo "Comment: ${{ github.event.comment.body }}"
