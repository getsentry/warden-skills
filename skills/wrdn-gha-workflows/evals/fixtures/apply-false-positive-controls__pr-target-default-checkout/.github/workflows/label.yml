name: Label
on:
  pull_request_target:
    types: [opened]
jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - run: ./scripts/label.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
