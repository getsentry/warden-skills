name: Run
on:
  workflow_dispatch:
    inputs:
      mode:
        type: choice
        options:
          - normal
          - "fast; rm -rf /tmp/data"
          - debug
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running in ${{ inputs.mode }} mode"
