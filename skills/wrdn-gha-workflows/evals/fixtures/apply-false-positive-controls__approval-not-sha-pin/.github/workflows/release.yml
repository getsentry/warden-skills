name: Release
on:
  workflow_dispatch:
jobs:
  release:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - uses: some-third-party/publish-action@v1
        with:
          token: ${{ secrets.NPM_TOKEN }}
