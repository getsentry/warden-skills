name: Label PR
on:
  pull_request_target:
    types: [opened]
jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - run: ./scripts/auto-label.sh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
