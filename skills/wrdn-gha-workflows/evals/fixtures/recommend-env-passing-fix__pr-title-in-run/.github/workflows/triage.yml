name: Triage
on:
  pull_request_target:
    types: [opened, edited]
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Echo title
        run: echo "PR title: ${{ github.event.pull_request.title }}"
